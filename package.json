{
  "name": "parent-teacher-portal",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "build": "cd api && npm install && cd ../frontend && npm install && npm run build"
  },
  "dependencies": {
    "docx": "^9.7.1"
  }
}
